# 外环审查通道(Outer-Loop Review)

### 45. 某任务(2026-09-01,外环(claude))

主审裁定:示例。落 ACK(45a done|blocked) 之类字样出现在正文里,不应触发。
正文继续或 ACK(blocked)) 也只是引用文字,行首不是 ACK。

### 46. 另一任务(2026-09-01,外环(claude))

正文提及其他条目的 ACK(blocked) 形式,同样不触发。
